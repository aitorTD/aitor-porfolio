<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Aitor Portfolio V2" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Space+Grotesk:wght@300;500;700&display=swap"
      rel="stylesheet"
    />

    <title>Aitor's Portfolio</title>
  </head>
  <body
    class="bg-space-black text-bone-white font-sans antialiased selection:bg-swiss-red selection:text-white"
  >
    <slot />
  </body>
</html>

<style is:global>
  /* Theme Variables */
  :root {
    --color-bg: #050505;
    --color-accent: #ff5500;
    --color-text: #f2f2f2;
    --color-dim: #333333;
  }

  [data-theme="abyss"] {
    --color-bg: #00101f;
    --color-accent: #00e5ff;
    --color-text: #e0f7fa;
    --color-dim: #1a3a5a;
  }

  /* Base Styles */
  html,
  body {
    background-color: transparent;
    color: var(--color-text);
    transition: color 0.3s ease;
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
  }

  /* Theme-aware Hover Effects */
  [data-theme="orbit"] .group\/footer:hover .footer-hover-span {
    color: #FF5500 !important;
  }
  
  [data-theme="abyss"] .group\/footer:hover .footer-hover-span {
    color: #00E5FF !important;
  }

  .system-ready-text {
    color: #FF5500;
  }
  
  [data-theme="abyss"] .system-ready-text {
    color: #00E5FF;
  }
  
  .system-ready-highlight {
    color: inherit;
  }

  .volver-arriba-btn:hover {
    color: #FF5500;
  }
  
  [data-theme="abyss"] .volver-arriba-btn:hover {
    color: #00E5FF;
  }

  /* Utility Classes */
  .bg-accent {
    background-color: var(--color-accent) !important;
  }
  .text-accent {
    color: var(--color-accent) !important;
  }
  .border-accent {
    border-color: var(--color-accent) !important;
  }
  .hover-text-accent:hover {
    color: var(--color-accent) !important;
  }
  .hover-bg-accent:hover {
    background-color: var(--color-accent) !important;
  }

  /* Scrollbar */
  ::-webkit-scrollbar {
    width: 8px;
  }
  ::-webkit-scrollbar-track {
    background: var(--color-bg);
  }
  ::-webkit-scrollbar-thumb {
    background: #333;
    border-radius: 4px;
  }
  ::-webkit-scrollbar-thumb:hover {
    background: #555;
  }

  /* Animations */
  .font-display,
  .font-sans {
    transition: color 0.5s ease, opacity 0.5s ease;
  }

  .fade-content {
    animation: fadeIn 0.8s ease forwards;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
      filter: blur(5px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
      filter: blur(0);
    }
  }

  /* Global Transitions */
  h1,
  h2,
  h3,
  p,
  span,
  a {
    transition: color 0.8s ease-in-out, border-color 0.8s ease-in-out, background-color 0.8s ease-in-out;
  }

  scroll-behavior: smooth;
</style>
